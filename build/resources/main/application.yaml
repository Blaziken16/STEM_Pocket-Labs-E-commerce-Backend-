jwt:
  secret: ${JWT_SECRET:"holiday-West-World-key-1516"}
  issuer: ${JWT_ISSUER:"pocketlabs-backend"}
  audience: ${JWT_AUDIENCE:"pocketlabs-users"}
  realm: ${JWT_REALM:"Access to PocketLabs API"}

ktor:
  deployment:
    port: ${PORT:8080}
    host: 0.0.0.0
  application:
    modules:
      - com.example.pocketLabs.ApplicationKt.module
