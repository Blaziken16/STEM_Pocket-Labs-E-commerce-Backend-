jwt:
  secret: "$JWT_SECRET"
  issuer: "$JWT_ISSUER"
  audience: "$JWT_AUDIENCE"
  realm: "$JWT_REALM"

ktor:
  deployment:
    port: "$PORT:8080"
    host: 0.0.0.0
  application:
    modules:
      - com.example.pocketLabs.ApplicationKt.module
